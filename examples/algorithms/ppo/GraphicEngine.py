import base64
from io import BytesIO

import numpy as np
from PIL import Image

"""
class GraphicEngine:


    def __init__(self, tiles_w, tiles_h, tile_size=32):

        self.tile_registry = {}
        self.objecty_registry = {}
        self.canvas = np.zeros(shape=(tiles_w * tile_size, tiles_h * tile_size, 3), dtype=np.uint8)

    def add_tile(self, tile_name, b64_img):

        self.tile_registry[tile_name] = np.array(Image.open(BytesIO(base64.b64decode(b64_img))))

    def d
"""
